# DeceptionDetector API Configuration
# ⚠️  IMPORTANT: Never commit this file to Git! It contains sensitive credentials.

# API Authentication (REQUIRED for Public API)
# Run: python generate_secrets.py to generate secure values
# Note: API_PASSWORD is stored in plain text (server-side only, never transmitted)
#       Clients must hash the password with SHA256 before sending to server
API_USERNAME=externalapiuser
API_PASSWORD=your_secure_password_here
JWT_SECRET=your_secure_jwt_secret_here_min_32_chars
JWT_ALGORITHM=HS256
JWT_EXP_SECONDS=3600

# Flask Configuration
FLASK_ENV=development
DEBUG_MODE=True

# Server Configuration
API_HOST=0.0.0.0
API_PORT=5000

# Rate Limiting (requests per minute)
RATE_LIMIT_ANALYSIS=20
RATE_LIMIT_TRAINING=5
RATE_LIMIT_DEFAULT=60

# CORS Allowed Origins (comma-separated for production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
